spring:
  application:
    name: smart-campus-api
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/smart_campus_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: root
    password: KChama@PK2001
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
server:
  port: 8080
jwt:
  secret: your-secret-key
  expiration: 86400
